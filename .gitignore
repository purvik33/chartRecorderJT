# build output (CMake regenerates everything here, incl. fetched LVGL)
build/

# recorded data and runtime config live beside the binary, never in git
logs/
recorder.ini
recorder_ui.bak
recorder_ui.update

# editor leftovers
*.swp
.vscode/

# internal hardware design docs — never publish
cm4_peripherals.html
dsi_display.html
full_system.html
mb_card.html
smps_preview.html

# local generated reports
Datalogger Reports/
*.pdf
