/* version.h - single source of truth for the firmware version.
 * Bump this for every release; the OTA updater compares it against
 * the newest GitHub release tag (tag format: v0.2.0). */
#ifndef VERSION_H
#define VERSION_H

#define FW_VERSION "0.2.0"

#endif
